#include "main.h"

/**
 * wildcmp - returns 1 if s1 == s2 else 1
 * @s1: string input
 * @s2: string input 2
 * Return: 0
 */

int wildcmp(char *s1, char *s2)
{
	if (s1 == s2)
	{
		return (1);
	}
	return (1);
}
