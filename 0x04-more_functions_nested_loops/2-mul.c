#include "main.h"

/**
 * _isdigit - check the code
 *
 *@c : ASCII equivalent
 *
 * Return: 1 if c  is a letter, lowercase or uppercase
 *
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	{
		return (0);
	}
}
